# Docker Configurations

This directory contains container configurations and orchestration files for the Pharma Signal platform:

- `docker-compose.yml`: Local PostgreSQL and Redis service orchestration (implemented in AHM-287 / AHM-288)
- `backend.Dockerfile`: Independent Dockerfile for backend deployment
- `frontend.Dockerfile`: Independent Dockerfile for frontend deployment
